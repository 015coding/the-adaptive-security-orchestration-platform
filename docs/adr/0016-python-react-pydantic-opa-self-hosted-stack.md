# Python, React, Pydantic, and OPA for the self-hosted MVP

The MVP uses a React and TypeScript interface with React Flow, plus a Python service using LangGraph and Pydantic for orchestration and validated contracts. OPA backs the Policy Engine; SQLite stores workflows, run metadata, audit records, and Findings, while encrypted Evidence Artifacts remain in the Permitted Workspace.
