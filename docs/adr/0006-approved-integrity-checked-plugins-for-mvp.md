# Approved, integrity-checked plugins for the MVP

Built-in Plugins are trusted, while external Plugins require administrator approval, integrity verification by checksum, and a Plugin Manifest before they may be used. An Approved Plugin must additionally be enabled by the active Crystal Flow, and the Policy Engine evaluates every invocation; the self-hosted MVP defers signed immutable registry artifacts to a future version.
