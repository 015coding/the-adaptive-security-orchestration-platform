# Local session adapter for Codex CLI

A local Session Adapter connects the orchestration service to the one running Codex CLI session, serializing node requests and returning structured results to the workflow graph. This removes manual handoffs while making the AI control point auditable.
