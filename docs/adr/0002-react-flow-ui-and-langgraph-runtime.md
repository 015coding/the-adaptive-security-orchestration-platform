# React Flow UI and LangGraph runtime

The platform uses React Flow to author and observe Assessment Workflows, and LangGraph to execute them. This separates the visual operator experience from the runtime while allowing every Workflow Node and Agent Message to be represented consistently in the Execution Trail.
