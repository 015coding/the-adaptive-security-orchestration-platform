# One sequential Codex CLI session

AI-powered Workflow Nodes use one currently running Codex CLI session and execute sequentially for the MVP. Each node receives structured input and returns standardized output under its Node Timeout and Resource Limits, with its invocation recorded in the Execution Trail; per-node processes and parallel execution are deferred.
