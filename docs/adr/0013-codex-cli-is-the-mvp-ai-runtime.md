# Codex CLI is the MVP AI runtime

The MVP uses only the currently running Codex CLI as its AI runtime and introduces no separate provider integration. Workflow execution must therefore record Codex CLI invocations in Model Call Records while preserving the established Scope, policy, and evidence controls.
