# Time-bounded Scopes by default

Every Scope has a required Scope Window by default, and workflow execution must stop or refuse to start outside it. An Owner may explicitly choose an unlimited resource setting where permitted, but that exception must remain visible, policy-controlled, and auditable.
