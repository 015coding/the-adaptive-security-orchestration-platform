# Configurable approval with invariant safety controls

The platform permits Approval Policy to be configured per action category, with Exploitation Attempts requiring human Approval by default. Disabling that requirement permits automated exploitation only in an Authorized Lab Environment; Scope, target validation, rate limits, Policy Engine checks, and Execution Trail recording remain mandatory regardless of configuration, preserving flexibility without creating a safety bypass.
