## Agent skills

### Issue tracker

Issues are tracked in the private GitHub repository. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the default triage labels. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context domain documentation lives at the repository root. See `docs/agents/domain.md`.
